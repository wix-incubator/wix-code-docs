{
  "name": "TagsQueryResult",
  "memberOf": "wix-blog-backend.Tags",
  "mixes": [],
  "callbacks": [],
  "extra": {},
  "clientId": "wix-blog-backend-tag.tags.tags-query-result",
  "docs": {},
  "relatedGuides": [],
  "properties": [
    {
      "name": "currentPage",
      "type": [
        {
          "nativeType": "number"
        }
      ],
      "docs": {
        "summary": "Returns the index of the current results page number.,,",
        "description": [
          "The currentPage is a zero-based index of the current page of results.",
          "",
          "The page size is defined by the `limit()` function, can be retrieved using the `pageSize` property, and navigating through pages is done with the `prev()` and `next()` functions.",
          "",
          "The `currentPage` property returns `undefined` if the query returned no results."
        ],
        "examples": [
          {
            "title": "currentPage example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "",
              "      const resultPage = results.currentPage; // 0",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      }
    },
    {
      "name": "items",
      "type": [
        {
          "complexType": {
            "nativeType": "Array",
            "typeParams": [
              {
                "referenceType": "wix-blog-backend.Tags.Tag"
              }
            ]
          }
        }
      ],
      "docs": {
        "summary": "Returns an array of `tags` that match the query.,,",
        "description": [
          "The current page of `tags` retrieved by the query.",
          "",
          "The page size is defined by the `limit()` function, can be retrieved using the `pageSize` property, and navigating through pages is done with the `prev()` and `next()` functions.",
          "",
          "When no items match the query, the `tags` array is empty."
        ],
        "examples": [
          {
            "title": "items example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      }
    },
    {
      "name": "length",
      "type": [
        {
          "nativeType": "number"
        }
      ],
      "docs": {
        "summary": "Returns the number of items in the current results page.,,",
        "description": [
          "The page size is defined by the `limit()` function, can be retrieved using the `pageSize` property, and navigating through pages is done with the `prev()` and `next()` functions."
        ],
        "examples": [
          {
            "title": "length example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "",
              "      const resultLength = results.length; // 20",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      }
    },
    {
      "name": "pageSize",
      "type": [
        {
          "nativeType": "number"
        }
      ],
      "docs": {
        "summary": "",
        "description": [
          ""
        ],
        "examples": [
          {
            "title": "pageSize example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      }
    },
    {
      "name": "query",
      "type": [
        {
          "referenceType": "wix-blog-backend.Tags.TagsQueryBuilder"
        }
      ],
      "docs": {
        "summary": "Returns the `TagsQueryBuilder` object used to get the current results.,,",
        "description": [
          "Use the `query` property to create and run a new query by chaining additional `TagsQueryBuilder` functions to it. You can only filter on properties that have not already been used in the previous query."
        ],
        "examples": [
          {
            "title": "query example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "      const newQuery = results.query;",
              "      newQuery.eq('id', 'some-id').find((newQueryResults) => {",
              "        const firstItem = newQueryResults.items[0];",
              "      });",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      }
    },
    {
      "name": "totalCount",
      "type": [
        {
          "nativeType": "number"
        }
      ],
      "docs": {
        "summary": "Returns the total number of items that match the query.,,",
        "description": [
          "The `totalCount` returns the total number of items that match the query, not just the number of items in the current page."
        ],
        "examples": [
          {
            "title": "totalCount example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "",
              "      const resultCount = results.totalCount; // 150",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      }
    },
    {
      "name": "totalPages",
      "type": [
        {
          "nativeType": "number"
        }
      ],
      "docs": {
        "summary": "Returns the total number of pages the query produced.,,",
        "description": [
          "The page size is defined by the `limit()` function, can be retrieved using the `pageSize` property, and navigating through pages is done with the `prev()` and `next()` functions."
        ],
        "examples": [
          {
            "title": "totalPages example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "",
              "      const resultPages = results.totalPages; // 3",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      }
    }
  ],
  "operations": [
    {
      "name": "hasNext",
      "params": [],
      "requiredFields": [],
      "ret": {
        "type": [
          {
            "nativeType": "boolean"
          }
        ],
        "doc": null
      },
      "docs": {
        "summary": "Indicates whether the query has more results.",
        "description": [
          ""
        ],
        "examples": [
          {
            "title": "hasNext example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      },
      "isVeloEvent": false
    },
    {
      "name": "hasPrev",
      "params": [],
      "requiredFields": [],
      "ret": {
        "type": [
          {
            "nativeType": "boolean"
          }
        ],
        "doc": null
      },
      "docs": {
        "summary": "Indicates whether the query has previous results.",
        "description": [
          ""
        ],
        "examples": [
          {
            "title": "hasPrev example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      },
      "isVeloEvent": false
    },
    {
      "name": "next",
      "params": [],
      "requiredFields": [],
      "ret": {
        "type": [
          {
            "complexType": {
              "nativeType": "Promise",
              "typeParams": [
                {
                  "referenceType": "wix-blog-backend.Tags.TagsQueryResult"
                }
              ]
            }
          }
        ],
        "doc": null
      },
      "docs": {
        "summary": "Retrieves the next page of query results.",
        "description": [
          "The `next()` function retrieves the next page of query results.",
          "",
          "The page size is defined by the `limit()` function, can be retrieved using the `pageSize` property, and navigating through pages is done with the `prev()` and `next()`  functions.",
          "",
          "If items are added or removed between calls to `next()` the values returned by `TagsQueryBuilder` may change."
        ],
        "examples": [
          {
            "title": "next example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "",
              "      let allItems = [];",
              "      while (results.hasNext()) {",
              "        nextResult = await results.next();",
              "        allItems = allItems.concat(nextResult.items);",
              "      }",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      },
      "isVeloEvent": false
    },
    {
      "name": "prev",
      "params": [],
      "requiredFields": [],
      "ret": {
        "type": [
          {
            "complexType": {
              "nativeType": "Promise",
              "typeParams": [
                {
                  "referenceType": "wix-blog-backend.Tags.TagsQueryResult"
                }
              ]
            }
          }
        ],
        "doc": null
      },
      "docs": {
        "summary": "Retrieves the previous page of query results.",
        "description": [
          "The `prev()` function retrieves the previous page of query results.",
          "",
          "The page size is defined by the `limit()` function, can be retrieved using the `pageSize` property, and navigating through pages is done with the `prev()` and `next()` functions.",
          "",
          "If items are added or removed between calls to `prev()` the values returned may change."
        ],
        "examples": [
          {
            "title": "prev example",
            "body": [
              "import { tags } from 'wix-blog-backend';",
              "//...",
              "tags",
              "  .queryTags()",
              "  .find()",
              "  .then(async (results) => {",
              "    if (results.items.length > 0) {",
              "      const items = results.items;",
              "      const firstItem = items[0];",
              "      const pageSize = results.pageSize;",
              "      const hasNext = results.hasNext();",
              "      const hasPrev = results.hasPrev();",
              "      const length = results.length;",
              "",
              "      let allItems = [];",
              "      while (results.hasPrev()) {",
              "        prevResult = await results.prev();",
              "        allItems = allItems.concat(prevResult.items);",
              "      }",
              "    } else {",
              "      // handle case where no matching items found",
              "    }",
              "  })",
              "  .catch((error) => {",
              "    console.error(error);",
              "  });",
              ""
            ]
          }
        ]
      },
      "isVeloEvent": false
    }
  ],
  "messages": []
}
